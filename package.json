{
  "name": "paperhands",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:ui": "npm run dev --prefix src/ui",
    "dev:api": "npm run dev --prefix src/api",
    "build:ui": "npm run build --prefix src/ui",
    "build:api": "npm run build --prefix src/api",
    "install:all": "npm install --prefix src/ui && npm install --prefix src/api"
  }
}
