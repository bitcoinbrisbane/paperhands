# API Server Configuration
PORT=8080

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=paperhands
DB_USER=paperhands
DB_PASSWORD=paperhands_dev

# Bitcoin Configuration
# BIP39 Mnemonic Seed (24 words) - IMPORTANT: This is a test seed, generate a new one for production
SEED=abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon art

# Independent Reserve API Configuration
# Get your API credentials from: https://portal.independentreserve.com/settings/api-keys
# Requires at least "Full Access" or "Restricted Withdrawals" role for withdraw operations
IR_API_KEY=your_api_key_here
IR_API_SECRET=your_api_secret_here
