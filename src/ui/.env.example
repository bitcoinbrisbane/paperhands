# API Base URLs
# For local development, point directly to the APIs
# For production, point to the reverse proxy paths (e.g., /api and /api2)

# TypeScript API (analytics, loans, etc.)
VITE_API_URL=http://localhost:8080

# Go API (auth, users)
VITE_API2_URL=http://localhost:8081
