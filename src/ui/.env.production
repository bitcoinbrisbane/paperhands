# Production API Base URLs
# These use nginx reverse proxy paths

# TypeScript API (analytics, loans, etc.) - proxied via /api/
VITE_API_URL=/api

# Go API (auth, users) - proxied via /api2/
VITE_API2_URL=/api2
